import "./chunk-BUSYA2B4.js";
import logger from "not-a-log";
function table(...parameters) {
  const original = logger.table(...parameters);
  const columnWidth = original.indexOf("\u252C") + 1;
  const trimmed = original.split("\n").map((line) => line.charAt(0) + line.slice(columnWidth)).join("\n");
  return trimmed;
}
export {
  table
};
//# sourceMappingURL=table.js.map